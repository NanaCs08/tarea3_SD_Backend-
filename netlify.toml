[build]
  functions = "functions"
  command = "npm install && npm run build"
  publish = "public"

[dev]
  framework = "#custom"
  command = "npm start"
  port = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
